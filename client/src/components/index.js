import React from 'react'

import Header from './Header'
import Body from './Body'

const Main = ({props})=>{
    return(
      <div>
        <Header />
        <Body />
      </div>
    );
}

export default Main